# Remove repository\(Github\) Instructions

#### Add remote repository

```bash
git remote add <name> <url>
```

#### Add remote refs usings local refs

```bash
git push <repository> <branch_name>
```

